export enum EntityType {
  Player = 'player',
  Enemy = 'enemy',
  Pod = 'pod',
  Bullet = 'bullet',
  Pickup = 'pickup',
  Particle = 'particle'
}

export enum Faction {
  Player = 'player',
  Enemy = 'enemy'
}
