export enum EntityType {
  Player = 'player',
  Enemy = 'enemy',
  Pod = 'pod',
  Bullet = 'bullet',
  Pickup = 'pickup',
  Particle = 'particle',
  Field = 'field',
  Drone = 'drone'
}

export enum Faction {
  Player = 'player',
  Enemy = 'enemy'
}
