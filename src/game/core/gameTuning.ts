import { gameSettings } from '../config/gameSettings';

export const playerTuning = gameSettings.tuning.player;
export const enemyDropTuning = gameSettings.tuning.enemyDrops;
export const podTuning = gameSettings.tuning.pods;
export const particleTuning = gameSettings.tuning.particles;
